# BVA Analysis for i18n / Locale Support

## Specification

The Messages class provides localized strings for the application. Each supported locaale has its own set of tranlsations. The current locale defaults to English and can be changed at runtime through setLocale. Adding a new locale requires only a new translation file and no changes to existing code.

---

## setLocale(Locale locale)

### Step 1 - Inputs and Outputs
- Input #1: target locale
- Output #1: current locale is updated
- Output #2: exception

### Step 2 - Data Types
- Input #1: Reference
- Output #1: Reference
- Output #2: Case

### Step 3 - Concrete Values
- Input #1: English, Korean, null, unsupported locale
- Output #1: locale is set
- Output #2: exception

### Step 4 - Test Cases
- Test Case 1: set locale to Korean
    - Method(s) under test: `setLocale(Locale locale)`, `getLocale()`
    - State of the system: locale is set to Korean
    - Expected output: getLocale() returns Korean
    - Implemented: yes

- Test Case 2: setLocale rejects null
    - Method(s) under test: `setLocale(Locale locale)`
    - State of the system: locale = null
    - Expected output: exception
    - Implemented: yes

---

## get(String key)

### Step 1 - Inputs and Outputs
- Input #1: message key
- Input #2: current locale
- Output #1: localized message
- Output #2: exception

### Step 2 - Data Types
- Input #1: String
- Input #2: Reference
- Output #1: String
- Output #2: Case

### Step 3 - Concrete Values
- Input #1: known key, missing key, null
- Input #2: English, Korean, unsupported locale
- Output #1: matching translation, fallback translation
- Output #2: exception

### Step 4 - Test Cases
- Test Case 3: get returns English message by default
    - Method(s) under test: `get(String key)`
    - State of the system: default locale, key is a known message key
    - Expected output: English message for the key
    - Implemented: yes

- Test Case 4: get returns Korean message after setLocale
    - Method(s) under test: `setLocale(Locale locale)`, `get(String key)`
    - State of the system: locale set to Korean, key is a known message key
    - Expected output: Korean message for the key
    - Implemented: yes

- Test Case 5: get with missing key throws exception
    - Method(s) under test: `get(String key)`
    - State of the system: key = "key.that.does.not.exist"
    - Expected output: exception
    - Implemented: yes

- Test Case 6: get with null key throws exception
    - Method(s) under test: `get(String key)`
    - State of the system: key = null
    - Expected output: exception
    - Implemented: yes

- Test Case 7: unsupported locale falls back to English
    - Method(s) under test: `setLocale(Locale locale)`, `get(String key)`
    - State of the system: locale set to an unsupported locale, key is a known message key
    - Expected output: English message for the key
    - Implemented: yess

---

## get(String key, Object... args)

### Step 1 - Inputs and Outputs
- Input #1: message key
- Input #2: format arguments
- Output #1: formatted message with arguments substituted

### Step 2 - Data Types
- Input #1: String
- Input #2: Collection
- Output #1: String

### Step 3 - Concrete Values
- Input #1: key with one placeholder
- Input #2: one argument
- Output #1: message with the argument substituted in place of the placeholder

### Step 4 - Test Cases
- Test Case 8: get with parameter substitutes value
    - Method(s) under test: `get(String key, Object... args)`
    - State of the system: key references a message with one placeholder, args has one value
    - Expected output: localized message with the argument substituted in place of the placeholder
    - Implemented: no
